function Login(): JSX.Element {
  return (
    <div className="Login">
      <button
        onClick={() => {}}
      >
        Login with Google
      </button>
    </div>
  );  
}

export default Login;
